using UnityEngine;

public class NumberScript : MonoBehaviour
{
    NumberScript rightThing;
    bool isNumber = true;
    void Start(){
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
